This project implements follow functional. There are layers that responsible to read matrix from file, to fill diagonal
of matrix required values, to write matrix to file. The project consist of several layers which implements layered
architecture. Each layer interacts only with neighbor layers.